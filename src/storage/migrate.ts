import type { Pool } from "pg";

/**
 * Stub — real migration logic will be added in a later task.
 * Keep in sync with the actual signature once implemented.
 */
export async function migrate(pool: Pool): Promise<void> {
  // no-op until migration module is implemented
}
