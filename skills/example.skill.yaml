name: greet_user
description: "用中文向用户打招呼。"
schema:
  type: object
  properties:
    name:
      type: string
      description: "用户的名字"
  required: ["name"]
action:
  type: template
  template: |
    你好，{{ name }}！欢迎使用 Navigate Agent。今天有什么可以帮你的？
